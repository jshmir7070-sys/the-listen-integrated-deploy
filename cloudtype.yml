name: the-listen-integrated-deploy
app:
  name: the-listen-integrated-deploy
  type: node
  build:
    install:
      - npm install
  run:
    command: node index.js
  env:
    - name: PORT
      value: 3000
