name: the-listen-integrated-deploy
app:
  name: the-listen-integrated-deploy
  type: node
  build:
    commands:
      - npm install
  run:
    command: node index.js
  env:
    - name: PORT
      value: 3000
